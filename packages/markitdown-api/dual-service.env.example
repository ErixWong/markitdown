# MarkItDown API + MCP 双服务配置

## 环境变量配置

复制此文件为 .env 并填写实际值：

```bash
cp dual-service.env.example .env
```

### 认证配置（两个服务共享）

# Bearer Token 认证（32字符以上，包含字母和数字）
# 两个服务使用相同的 Token，客户端只需配置一次
MARKITDOWN_API_KEY=your-secret-token-32-chars-min-with-mixed-123

### OCR 配置（仅 MCP 服务需要）

# LLM API Key（用于 OCR 图像识别）
MARKITDOWN_OCR_API_KEY=sk-your-openai-api-key-here

# OCR 模型选择
MARKITDOWN_OCR_MODEL=gpt-4o

# API 基础地址（OpenAI 或兼容服务）
MARKITDOWN_OCR_API_BASE=https://api.openai.com/v1

# OCR 超时时间（秒）
MARKITDOWN_OCR_TIMEOUT=120

### 可选：服务独立认证

# 如果希望 API 和 MCP 使用不同 Token，取消以下注释：
# MARKITDOWN_API_TOKEN=api-only-secret-token-here
# MARKITDOWN_MCP_TOKEN=mcp-only-secret-token-here

### 高级配置

# 最大文件大小（MB）
MARKITDOWN_MAX_FILE_SIZE_MB=100

# 最大并发任务数
MARKITDOWN_MAX_CONCURRENT=3

# CORS 允许来源（API 服务）
MARKITDOWN_CORS_ORIGINS=*
